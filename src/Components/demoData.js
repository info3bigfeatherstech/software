/**
 * Default seed data for legacy localStorage-backed tabs (Receive, Stock, Returns).
 * Empty arrays — persisted data in localStorage takes precedence after first load.
 */

export const INITIAL_WAREHOUSES = [];
export const INITIAL_VENDORS = [];
export const INITIAL_PRODUCTS = [];
export const INITIAL_PURCHASES = [];
export const INITIAL_CREDIT_NOTES = [];
export const INITIAL_BILLS = [];
export const INITIAL_SHOPS = [];
